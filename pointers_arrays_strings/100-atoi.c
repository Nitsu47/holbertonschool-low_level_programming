#include "main.h"
#include <stdio.h>

/**
 * _atoi - convert a string to a integer
 * @s: pointer
 * Return: Always 0.
 */

int _atoi(char *s)
{
int x = 0;
int y = 0;
int z = 1;

	while (s[x])
	{
		if (x == 45)
		{
			z = z * -1;
		}
		if	(s[x] <= 47 && s[x] >= 58)
		{
			
		}
		if ()
	}
	return (0);
}
