#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: pointer
 * @s2: pointer
 * Return: strings.
 */

int _strcmp(char *s1, char *s2)
{
int la;
int lb;

	for (la = 0; s1[la] != '\0'; la++)
	{
	}
	for (lb = 0; s2[lb] != '\0'; lb++)
	{
	}
	if (s1[la] > s2[lb])
	{
		return (la);

	if (s1[la] < s2[lb])
	{
		return (la *= -1);
	}
	}
	else
	return (0);
}
